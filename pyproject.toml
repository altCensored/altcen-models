[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "altcen-models"
version = "2026.6.19.1"
description = "Shared SQLAlchemy ORM models for altCensored"
requires-python = ">=3.8"
dependencies = [
    "sqlalchemy>=2.0",
]
